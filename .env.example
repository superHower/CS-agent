# ============================================================
# CS-Agent 环境变量示例
# 复制为 .env 后填入真实值，.env 已在 .gitignore 中，不会提交
# ============================================================

# ── Redis（默认本地无密码，有密码才需要填）────────────────────
# REDIS_PASSWORD=your_redis_password
